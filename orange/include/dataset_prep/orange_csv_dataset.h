#ifndef __ORANGE_CSV_DATASET_H
#define __ORANGE_CSV_DATASET_H

#include<matrix.h>

void orange_dataset_csv_remove_columns(const char* source_filename,const char* destination_filename, set_string)
{
    if(!source_filename || !dataset_csv_remove_columns || !columns)
    {
        goto handle_error;
    }
    if(!columns)
    {
        goto handle_error;
    }
    
    handle_error:
    // some code
    return NULL;
}

#endif